# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'AppForLearn' do

  pod 'lottie-ios'
  pod 'SnapKit', '~> 4.0.0'

  target 'AppForLearnTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppForLearnUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
